apiVersion: v1
kind: Service
metadata:
  name: {{ include "it-tools.fullname" . }}
spec:
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
  selector:
    app: {{ include "it-tools.name" . }}