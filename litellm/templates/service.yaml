apiVersion: v1
kind: Service
metadata:
  name: {{ include "litellm.fullname" . }}
spec:
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 4000
  selector:
    app: {{ include "litellm.name" . }}