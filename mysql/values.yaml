replicaCount: 1

image:
  repository: mysql
  tag: 8.4
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 3306

rootPassword: changeme123
database: ""

persistence:
  enabled: true
  size: 5Gi
  storageClass: ""

resources:
  requests:
    memory: 512Mi
    cpu: 200m
  limits:
    memory: 1Gi
    cpu: 500m