apiVersion: v1
kind: Service
metadata:
  name: {{ include "security.fullname" . }}
  labels:
    app: {{ include "security.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ include "security.name" . }}
