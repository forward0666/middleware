apiVersion: v1
kind: Service
metadata:
  name: {{ include "cloudflare-logs-consumer.fullname" . }}
  labels:
    app: {{ include "cloudflare-logs-consumer.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ include "cloudflare-logs-consumer.name" . }}
