apiVersion: v1
kind: Service
metadata:
  name: {{ include "domain.fullname" . }}
  labels:
    app: {{ include "domain.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ include "domain.name" . }}
