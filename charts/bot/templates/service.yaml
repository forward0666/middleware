apiVersion: v1
kind: Service
metadata:
  name: {{ include "bot.fullname" . }}
  labels:
    app: {{ include "bot.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ include "bot.name" . }}
