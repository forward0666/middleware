apiVersion: v1
kind: Service
metadata:
  name: {{ include "cloudflare-logs-producer.fullname" . }}
  labels:
    app: {{ include "cloudflare-logs-producer.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ include "cloudflare-logs-producer.name" . }}
